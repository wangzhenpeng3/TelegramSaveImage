import path from "path";

export enum Status {
    success = 'success',
    progress = 'progress',
    error = 'error'
}

export enum ParseHtmlTag {
    image = 'img'
}
export enum DownloadPath {
    image = '/download',
    config = `/config`,
    logs = '/logs'
}

export enum HTTPCode {

}