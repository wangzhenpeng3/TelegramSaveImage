export enum Status {
    success = 'success',
    progress = 'progress',
    error = 'error'
}

export enum ParseHtmlTag {
    image = 'img'
}

export enum DownloadPath {
    image = '/download'
}

export enum HTTPCode {

}